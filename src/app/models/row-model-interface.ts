export interface RowModel {
    headerName: string,
    field: string,
    cellRenderer: string,
    autoHeight?: boolean,
    checkboxSelection?: boolean,
    headerCheckboxSelection?: boolean,
    width: number
}